registry=https://registry.npmjs.org/

# pnpm 12's default supply-chain policy rejects installing packages published
# within the last ~24h. This project intentionally tracks very recently
# published versions of its own deps, so that policy is relaxed to 0 here.
minimum-release-age=0
